📦 Описание проекта

Данный проект представляет собой Telegram-бота, который принимает сообщения от пользователей, анализирует их текст, ищет заданные ключевые слова и сохраняет результаты в формате **JSON**.
Бот может использоваться для мониторинга чатов, анализа обратной связи или фильтрации сообщений по темам.

---

⚙️ Основные возможности

* Принимает сообщения от пользователей.
* Ищет ключевые слова с помощью библиотеки `flashtext` (или `re`).
* Автоматически сохраняет найденные ключевые слова и текст сообщения в файл `data.json`.
* Поддерживает несколько языков и любые наборы ключевых слов.

---

🧩 Используемые библиотеки

1. python-telegram-bot** — работа с Telegram API.
2. flashtext** — быстрый поиск ключевых слов.
3. json — сохранение данных в формате JSON.


---

🚀 Установка

1. Убедитесь, что у вас установлен Python 3.9+
2. Установите зависимости:

   ```bash
   pip install python-telegram-bot flashtext
   ```
3. Создайте файл `.env` (или просто впишите токен напрямую в код) и добавьте туда ваш токен Telegram бота:

   ```
   TELEGRAM_TOKEN=ваш_токен_от_BotFather
   ```

---

🧠 Пример работы

Пользователь отправляет сообщение:

"Этот бот сохраняет JSON данные по ключевым словам.”

Результат сохраняется в `data.json`:

json
{
    "user": "username",
    "message": "Этот бот сохраняет JSON данные по ключевым словам.",
    "keywords": ["бот", "JSON"]
}
```

---
📂 Структура проекта


telegram-bot-keyword-sorter/
│
├── main.py             # Основной файл с кодом бота
├── data.json           # Файл, куда сохраняются результаты
├── requirements.txt     # Список зависимостей
└── README.txt           # Документация проекта
```

---

💡 Идеи для улучшения

Добавить возможность редактировать список ключевых слов через Telegram.
Сохранять данные не только в JSON, но и в базу данных (например, SQLite).
Добавить панель администратора для просмотра статистики.
Интеграция с Google Sheets для анализа сообщений.

---

